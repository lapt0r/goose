# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: returntocorp/semgrep-agent:v1

steps:
- script: semgrep-agent --config auto
  displayName: 'invoke semgrep-agent with auto config'
